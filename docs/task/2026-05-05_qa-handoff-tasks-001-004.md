# [QA 핸드오프] 2026-05-04 라운드 4개 task 완료 — QA 인수 대기

> **상태**: 🟢 코드 작업 100% 완료 / **QA 인수 대기**
> **작성**: 2026-05-05 (시니어 책임자)
> **대상**: QA팀
> **출시 게이트**: QA 통과 후 정식 배포

---

## 0. 한 줄 요약

> 2026-05-04 problem 문서 4건(001 인증 흐름 / 002 Functions 운영 강화 / 003 jurisdictions 셀프-서비스 / 004 규칙서 사용자 노출 갭) 모두 코드 작업 완료. 본 세션에서 신규 커밋 10건 추가. 모든 task 의 SPEC 합격 기준 자가 점검 통과 — 이제 QA팀의 실 환경 검증·시연 단계.

---

## 1. 본 세션 신규 커밋 10건 (Wave 1 + Wave 2)

| SHA | 메시지 | Task |
|---|---|---|
| `dbb34b7` | feat(001-copy): grant_messages 인증 카피 + copy-deck §6.1 관리자 카피 가이드 | 001 |
| `ee401e9` | feat(001-admin): 인증 신청 큐 탭 + 대시보드 대기 카드 | 001 |
| `4f8f894` | feat(001-broker): 중개사 인증 신청 페이지 + 상태 배너 + service 5메서드 | 001 |
| `05353dc` | feat(001-backend): 중개사 인증 신청·승인 callable 3개 + Rules + 인덱스 | 001 |
| `1d98d09` | docs(task-003): 중개사 영업 동네 셀프-서비스 완료 보고서 | 003 |
| `9dd34a9` | feat(infra): callable catch 블록에 CloudFunctionsGuard 적용 | 002 Phase 4 |
| `e997f75` | chore(functions): Node 20→22 + firebase-functions v5→v6 | 002 Phase 6 |
| `551d30e` | feat(infra): functions exports/deploy diff 모니터 스크립트 | 002 Phase 5 (+ 003 일부 흡수) |
| `c08e0c5` | feat(004-gap1+2): 매도자 이의 진입점 + 자진 만료 위젯 + Rules 보강 | 004 |
| `58cde15` | feat(004-gap1): PriorityAppealPage broker→broker/seller 일반화 | 004 |

> **참고**: pre-commit hook이 modified 파일을 자동 staging 하는 동작으로 일부 task 산출물이 다른 커밋에 흡수됨 (예: Phase 4 가드 모듈 일부, Task 003 일부). 코드 동작·머지 영향 0, 작성자 이력 추적 시 참고.

---

## 2. Task별 산출물 인벤토리

### 2.1 Task 001 — 중개사 인증 신청·승인 흐름 (P0)

**SPEC**: [`2026-05-04_001-broker-verification-flow.md`](2026-05-04_001-broker-verification-flow.md)

#### 백엔드
- **신규 컬렉션**: `brokerVerificationRequests/{requestId}` (스키마 SPEC §2.1.1)
- **`firestore.rules`** 보강:
  - `brokerVerificationRequests` 룰 신설 (read: 본인 또는 admin / write: callable 전용)
  - `brokers` `allow update` — 본인은 `verified`/`licenseVerified`/`licenseVerifiedAt` 자가 토글 차단
- **`firestore.indexes.json`**: `brokerVerificationRequests (status ASC, requestedAt DESC)` 1건 추가
- **`functions/index.js`** 신규 callable 3개 + helper 1개:
  - `submitBrokerVerificationRequest` — 본인 신청, 트랜잭션 + 중복 차단
  - `approveBrokerVerification` — admin 승인, 4 doc 동시 갱신 (request·brokers·broker_eligibility + audit)
  - `rejectBrokerVerification` — admin 반려, 사유 5~200자
  - `buildBrokerEligibilityPayload` — pure helper (트랜잭션 친화), `recomputeSingleBrokerEligibility` 도 본 helper 사용 → 이중 정의 0
- audit 이벤트 3종: `broker_verification_requested` / `broker_verification_approved` / `broker_verification_rejected`
- FCM: 트랜잭션 외부 호출, 실패 시 audit `notification_failed:true` 기록

#### Flutter — 중개사
- `lib/api_request/firebase_service.dart` 신규 5메서드 (모두 `CloudFunctionsGuard` 적용)
- `lib/screens/broker/broker_verification_apply_page.dart` (신규) — 한 화면 = 한 결정. JurisdictionPicker 임베드.
- `lib/widgets/broker_verification_status_banner.dart` (신규) — pending/rejected/approved 분기, 24h SharedPreferences dismiss
- `lib/screens/broker/mls_broker_dashboard_page.dart` — `_showVerificationGuideSheet` `onPressed` 교체 + 헤더 아래 배너 통합

#### Flutter — 관리자
- `lib/screens/admin/admin_broker_management.dart` — TabBar 2탭 분리 (전체/인증 신청), 신청 상세 BottomSheet, 반려 사유 다이얼로그 (5~200자 검증, 80세 화법 placeholder)
- `lib/screens/admin/admin_dashboard.dart` — "인증 신청 대기 N건" 카드 (N>0 시 warning 강조)

#### 카피
- `lib/constants/grant_messages.dart` — `verifyApply*`/`verifyBanner*`/`verifyAdmin*` 17개 상수
- `docs/common/copy-deck.md` §6.1 — 운영자→사용자 통지 가이드 신설 (반려 사유 모범/금지 5쌍)

#### 검증 합격 기준 (SPEC §4) — 자가 점검 통과
- [x] 신규 가입 중개사 → 신청 폼 → jurisdictions 1~5건 선택 → 제출 → `brokerVerificationRequests/{newId}` 생성
- [x] 동일 brokerUid pending 1건 시 두 번째 신청 → `failed-precondition: duplicate_pending`
- [x] 관리자 대시보드 "인증 신청 대기 N건" 실시간 카드
- [x] 승인 → 4 doc 동시 갱신 (request + brokers + broker_eligibility 2필드)
- [x] 본인이 `brokers.verified` 자가 토글 시 PERMISSION_DENIED
- [x] audit 이벤트 3종 1:1 기록
- [x] 사용자 노출 카피에 영문 식별자 0건

---

### 2.2 Task 002 — Cloud Functions 배포 운영 강화 (P0)

**SPEC**: [`2026-05-04_002-functions-deployment-hardening.md`](2026-05-04_002-functions-deployment-hardening.md)

#### Phase 3 — sendPushNotification region 마이그 (코드만)
- `functions/index.js` line 79 부근: `onDocumentCreated` 에 `region: "asia-northeast3"` 추가
- 운영팀 후속 명령 1줄 주석으로 기록: `firebase functions:delete sendPushNotification --region us-central1 --force`

#### Phase 4 — NOT_FOUND 가드
- 신설: `lib/utils/cloud_functions_guard.dart`
  - `CloudFunctionsGuard.throwAsUserFacing` + `CloudFunctionsUserFacingException`
  - 80세 노인 화법 카피 3종 (NOT_FOUND / UNAVAILABLE / UNAUTHENTICATED)
- **17곳 catch 블록** 패치 (서비스 9 + UI 5 + inline 2 + loop silent 1)
- 도메인 에러(`failed-precondition` 등)는 그대로 통과 → 기존 `describeReason` 흐름 100% 보존

#### Phase 5 — diff 모니터 스크립트
- 신설: `tools/check_functions_deployment_diff.mjs` (Node 20+ ESM, npm 의존성 0)
- 4 모드: `--pre-deploy` / `--post-deploy` / `--ci` / `--help`
- exit 0(일치) / 1(불일치) / 2(firebase CLI 없음) / 3(인자 오류)
- Windows/POSIX 호환

#### Phase 6 — Node 22 + firebase-functions v6
- `functions/package.json`: engines.node `20→22`, firebase-admin `^12→^13`, firebase-functions `^5→^6`
- `npm install` 통과
- Breaking change 5개 항목 정적 검증 클린 (request.auth/Timestamp/global crypto 등)

#### Phase 1·2 — 보류
- Phase 1 (사용자 검증 게이트): QA팀이 별도 진행 (본 핸드오프와 통합)
- Phase 2 (CI/CD GitHub Actions): owner 권한 필요 → 시니어 책임자/운영팀 후속 라운드

---

### 2.3 Task 003 — 중개사 영업 동네(jurisdictions) 셀프-서비스 (P0)

**SPEC**: [`2026-05-04_003-broker-jurisdictions-self-service.md`](2026-05-04_003-broker-jurisdictions-self-service.md)
**완료 보고서**: [`2026-05-04_003-broker-jurisdictions-self-service-DONE.md`](2026-05-04_003-broker-jurisdictions-self-service-DONE.md)

#### 신규 파일
- `lib/constants/jurisdictions.dart` — 시·도→시·군·구 catalog (서울 25 + 광역시 + 경기 31 + 세종, ~90개 우선)
- `lib/widgets/jurisdiction_picker/jurisdiction_picker.dart` — 다중 선택 다이얼로그 + `showJurisdictionPicker` 헬퍼
- `lib/widgets/jurisdiction_picker/jurisdiction_empty_banner.dart` — 빈 상태 경고 배너

#### 수정
- `lib/api_request/firebase_service.dart` — `updateBrokerJurisdictions(brokerId, codes)` (3중 검증)
- `lib/screens/broker/broker_settings_page.dart` — 영업 지역 섹션 + 칩 + 즉시 저장 + 빈 배너
- `lib/screens/broker/broker_signup_page.dart` — Phase 2 가입 폼 통합 (jurisdictions 1개 이상 필수)
- `lib/screens/broker/mls_broker_dashboard_page.dart` — 대시보드 헤더 빈 상태 배너 (verified=true && empty)
- `functions/index.js` — `onBrokerJurisdictionsUpdated` onUpdate 트리거 (정렬 JSON 비교로 무한 루프 방지)
- `firestore.rules` — `isValidJurisdictions()` 헬퍼 + brokers create/update 적용
- `lib/constants/grant_messages.dart` — jurisdiction 카피 11종
- `docs/common/copy-deck.md` — §2.8 JurisdictionPicker 섹션

#### 후속 위임
- catalog 250+ 전체 시·군·구 확장 → v1.5 운영 phase + `tools/data/lawd_codes.json` 동기 (현재 ~90개 우선)
- Phase 3 사무소 도로명 주소 자동 추론 → 별도 UX 라운드
- Phase 4 관리자 강제 수정 → 분쟁 처리 라운드

---

### 2.4 Task 004 — 규칙서 v1.5.0 사용자 노출 갭 봉합 (P0)

**SPEC**: [`2026-05-04_004-rulebook-v1.5.0-user-exposure.md`](2026-05-04_004-rulebook-v1.5.0-user-exposure.md)

#### 갭 1 — 매도자 측 "이상해요" 이의 제기 진입점
- `lib/screens/broker/priority_appeal_page.dart` — 옵션 A 일반화 (`brokerUid` → `actorUid` + `AppealActorRole {broker, seller}` + `preselectedGrantId`)
- `lib/api_request/priority_grant_service.dart` — `watchBySeller(sellerUid)` 1줄 추가
- `lib/screens/seller/mls_seller_dashboard_page.dart` — 헤더 IconButton(`Icons.report_problem_outlined`) + audit row 화이트리스트(6종) 진입 + bottom sheet
- `lib/screens/broker/mls_broker_dashboard_page.dart` — 호출자 `actorUid` 동시 갱신 (호환 유지)
- `firestore.indexes.json` — `(sellerUid, status, grantedAt)` 인덱스 1건 추가

#### 갭 2 — 중개사 평상시 자진 만료 진입점
- `lib/widgets/revoke_own_grant_confirm_dialog.dart` (신규) — 2단계 다이얼로그 ("놓기" → "정말 놓기" 라벨 차별화로 오탭 방어)
- `lib/screens/broker/_widgets/broker_property_card.dart` — 상단 Row에 `PopupMenuButton(more_vert)` 추가 (본인 active grant 보유 시에만 노출)

#### Firestore Rules 보강 (중요)
- `firestore.rules:458-505` `priority_appeals` create
  - 분기 1 (`listing_mode_dispute`): `mlsProperties.userId` 또는 `grant.brokerUid` 검증
  - 분기 2 (`grant_decision`/`other`): `grant.brokerUid` 또는 `grant.sellerUid` 검증
- 매도자 진입점 노출 시 임의 사용자가 남의 매물에 이의 거는 권한 갭 봉합

#### 카피 동기 (3-레이어)
- `lib/constants/grant_messages.dart` — 신규 10건 (갭1 5건 + 갭2 5건), 재사용 2건
- `docs/common/copy-deck.md` §2.7 5행 추가 + §2.7.1 신설 (revoke 7행)

#### 후속 위임
- 매수자(buyer) 진입점 (P2 백로그)
- 매도자 이의 남발 server-side rate limit (P2)
- `priority_appeal_page.dart` `lib/screens/_shared/` 이동 (P3)

---

## 3. 정적 검증 결과

| 검증 | 결과 | 비고 |
|---|:---:|---|
| `dart analyze` 전체 | ✅ error 0 / warning 0 / info 136 | info 는 모두 prefer_const_constructors 등 기존 코드 lint, 본 세션 변경분 0건 |
| `node -c functions/index.js` | ✅ 통과 | (출력 없음) |
| 본 세션 변경 파일 `dart analyze` | ✅ No issues found | 신규/수정 ~20개 파일 |
| 80세 노인 테스트 카피 영문 식별자 | ✅ 0건 | grep `eligibility|verified|pending|approved|score|matching` 사용자 노출 0 |
| 절대 금지 패턴 (00-INDEX §5) | ✅ 0건 | 보수 분배·targetBrokerIds 직접 변형·약관-only 강제·테스트 없는 만료 등 0 |

---

## 4. QA 검증 시나리오 (SPEC 기반 통합)

### 4.1 Task 001 — 중개사 인증 흐름 (SPEC §7)
1. **정상 경로**: 신규 가입 중개사 → "이 매물 받기" → 미인증 시트 → "인증 신청하기" → 사무소 정보 read-only 카드 + jurisdictions 선택 → 제출 → 신청 완료 시트 → 대시보드 복귀 후 `verifyBannerPending` 노출 → 관리자 승인 → FCM 푸시 도착 + `verifyBannerApprovedToast` 1회 노출 → "이 매물 받기" 재시도 정상 진행
2. **중복 차단**: 동일 계정 두 번째 신청 → "이미 신청이 접수되었어요" SnackBar
3. **반려 → 재신청**: 관리자 반려 사유 입력 → FCM 본문에 사유 포함 → 배너 `verifyBannerRejectedPrefix` + 사유 + `[다시 신청]` 버튼 → 재진입 → 새 pending doc 생성
4. **자가 토글 차단**: 콘솔에서 `brokers/{uid}.verified=true` 직접 update → PERMISSION_DENIED
5. **동시 승인 race**: 관리자 2명 동시 승인 클릭 → 한쪽 성공, 다른쪽 "이미 처리된 신청이에요"
6. **eligibility 동기화**: 승인 직후 `broker_eligibility/{uid}.licenseStatus='verified'` + `jurisdictions` 즉시 반영 (트랜잭션 인라인)

### 4.2 Task 002 — Functions 운영 강화 (SPEC §7)
1. **NOT_FOUND 가드**: 임의 callable 의도적 미배포 → 호출 → 사용자에게 "잠시 점검 중입니다. 1~2분 뒤 다시 눌러 주세요." (영문 에러 노출 0)
2. **매물받기 회귀**: 중개사가 매물 카드 → "매물받기" → grant 발급 또는 자격/한도 도메인 에러 (NOT_FOUND 0)
3. **FCM 푸시**: 매도자 신규 매물 등록 → 30초 이내 단 1회 푸시 (asia-northeast3 단일 리전)
4. **diff 스크립트**: `node tools/check_functions_deployment_diff.mjs --help` 정상 출력

### 4.3 Task 003 — jurisdictions 셀프-서비스 (SPEC §7)
1. **정상 흐름**: 본인정보 페이지 → "영업 가능 지역" 섹션 → [지역 추가/편집] → 시·도 + 시·군·구 선택 → 즉시 저장 → SnackBar "영업 지역이 저장되었어요" → 30초 내 `broker_eligibility` 동기화 → 강남구 매물 신청 시 `jurisdictionMatch=true`
2. **에러**: 0개 저장 시 [저장] 비활성화 / 6개 시도 시 차단 / Rules 우회 시 PERMISSION_DENIED

### 4.4 Task 004 — 사용자 노출 갭 (SPEC §7)
1. **매도자 정상 흐름**: 매도자 헤더 "이상해요" 버튼 → PriorityAppealPage(role: seller) → 본인 매물 active grant 드롭다운 → 사유 입력 → 제출 → FCM 도착
2. **audit timeline 진입**: grant_issued 행 우측 "이상해요로 알리기" → bottom sheet → 페이지 진입 시 해당 grant 자동 선택
3. **권한 외부 차단**: 매도자 A가 매물 B(소유주 C)의 grantId로 직접 append 시도 → permission-denied
4. **중개사 자진 만료**: ⋮ → "이 매물 놓기" → 1단계 [놓기] → 2단계 [정말 놓기] → callable 성공 → 카드 뱃지 갱신 + SnackBar
5. **자진 만료 직후 재신청**: 4 직후 동일 매물 "이 매물 받기" → backend `revoke_cooldown` → "방금 놓은 매물은 24시간 뒤에 다시 받을 수 있어요"

---

## 5. 운영 배포 절차 (QA 통과 후)

```bash
# 1. Firestore Rules + Indexes
firebase deploy --only firestore

# 2. Cloud Functions (Node 22 — 본 라운드 마이그)
cd functions && npm install && firebase deploy --only functions --force && cd ..

# 3. (Phase 3 후속) us-central1 잔존 sendPushNotification 강제 삭제
firebase functions:delete sendPushNotification --region us-central1 --force
firebase functions:list | grep sendPushNotification  # asia-northeast3 1건만 확인

# 4. 메인 웹 빌드 + 배포
flutter build web --release \
  --dart-define=JUSO_API_KEY=xxx \
  --dart-define=DATA_GO_KR_SERVICE_KEY=yyy
firebase deploy --only hosting:main

# 5. 관리자 콘솔 빌드 + 배포 (별도 바이너리)
flutter build web --release \
  --target=lib/main_admin.dart \
  --output=build/web_admin
firebase deploy --only hosting:admin

# 6. (Phase 5 후속) 배포 후 diff 검증
node tools/check_functions_deployment_diff.mjs --post-deploy
```

---

## 6. 미해결 / 후속 라운드 위임 항목

| 출처 | 항목 | 우선순위 | 위임 |
|---|---|:---:|---|
| Task 001 Phase 2 | 면허증 사진 업로드 (Storage Rules + 미리보기) | P1 | 별도 라운드 |
| Task 001 Phase 2 | 외부 면허 검증 API 연동 (국토부·KAR) | P1 | 별도 라운드 |
| Task 001 | approved 1회 토스트 SharedPreferences → `brokers/{uid}.notifications.verifyApprovedSeenAt` 전환 | P3 | 멀티 디바이스 운영 시점 |
| Task 002 Phase 2 | CI/CD GitHub Actions 자동 배포 (FIREBASE_DEPLOY_TOKEN secret 등록) | P1 | owner 권한 필요 — 시니어/운영팀 |
| Task 002 Phase 2 | OIDC (Workload Identity Federation) 전환 | P3 | Phase 6 안정화 후 분기 내 |
| Task 003 | catalog 250+ 전체 시·군·구 확장 (`tools/data/lawd_codes.json`) | P1 | v1.5 운영 phase |
| Task 003 Phase 3 | 사무소 도로명 → 시·군·구 자동 추론 (vworld geocoding) | P2 | 별도 UX 라운드 |
| Task 003 Phase 4 | 관리자 강제 수정 (jurisdictions override) | P2 | 분쟁 처리 발생 시 |
| Task 004 | 매수자(buyer) 이의 진입점 | P2 | SPEC §9 백로그 |
| Task 004 | 매도자 이의 남발 server-side rate limit | P2 | 운영 데이터 누적 후 |
| Task 004 | `priority_appeal_page.dart` `_shared/` 이동 | P3 | mv 단순 작업 |

---

## 7. QA 인수 체크리스트

### 7.1 환경 점검
- [ ] staging 프로젝트(`houseproject-18f44`) 접근 권한
- [ ] 관리자 계정 1개 + admin claim 설정
- [ ] 중개사 계정 2개 (신규 가입 + 인증 완료)
- [ ] 매도자 계정 1개 (활성 매물 1건 이상)

### 7.2 시나리오 검증 (SPEC 기반)
- [ ] Task 001 6 시나리오 (§4.1)
- [ ] Task 002 4 시나리오 (§4.2)
- [ ] Task 003 2 시나리오 (§4.3)
- [ ] Task 004 5 시나리오 (§4.4)

### 7.3 80세 노인 테스트 (사용성)
- [ ] 60대 비전문가 3명 시연 ([usability-test-protocol.md](usability-test-protocol.md))
- [ ] 모든 화면 영문 식별자 0건
- [ ] 한 화면 의사결정 ≤2

### 7.4 회귀 검증
- [ ] 기존 매물받기 흐름 (Task 002 NOT_FOUND 가드 적용 후 도메인 에러 정상 분기)
- [ ] 기존 broker 이의 제기 흐름 (Task 004 일반화 후 default broker 호환)
- [ ] 5개 한도 시점 자진 만료 (Task 004 갭2 신규 + 기존 PriorityCapDialog 공존)
- [ ] 기존 broker_eligibility 동기 (Task 001 helper 리팩터 후 기존 흐름 영향 0)

---

## 8. 다음 핸드오프 트리거

```
QA 인수 결과 수령 시점:

1. 합격 기준 100% 통과 → 운영 배포 (§5 절차) → v1.4.1 정식 출시
2. 일부 항목 fail → 시니어 책임자가 fail 항목별로 hot-fix 라운드 디스패치
3. UX 개선 권고 발견 → 후속 라운드 백로그 등재 (§6)
```

---

**작성**: 시니어 책임자 (총괄)
**상태**: 🟢 코드 작업 100% 완료 — QA 인수 대기
**다음 트리거**: QA팀 인수 결과 수령 시점
