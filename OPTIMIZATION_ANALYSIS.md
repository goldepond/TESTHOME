# 📋 프로젝트 최적화 및 삭제 항목 분석

> 작성일: 2025-01-XX  
> 전체 프로젝트 파일 점검 결과

---

## 🔴 삭제 가능한 파일 (즉시 삭제 가능)

### 1. 빈 폴더
- [ ] `lib/database/` - 빈 폴더 (DatabaseHelper 미사용)

### 2. 템플릿/미사용 파일
- [ ] `lib/constants/app_constants_template.dart` - 템플릿 파일 (사용 안 함)
- [ ] `public/index.html` - Firebase Hosting 기본 페이지 (사용 안 함)

### 3. 테스트/샘플 파일
- [ ] `web/test_markers.html` - 네이버 지도 마커 테스트 파일 (개발용)
- [ ] `test/widget_test.dart` - 기본 테스트 파일 (실제 테스트 없음)
- [ ] `assets/sample_house.jpg` - 샘플 이미지 (코드에서 사용 안 함)
- [ ] `assets/sample_house2.jpg` - 샘플 이미지 (코드에서 사용 안 함)
- [ ] `assets/sample_house3.jpg` - 샘플 이미지 (코드에서 사용 안 함)
- [ ] `assets/assets.zip` - 압축 파일 (원본 파일 존재)

### 4. 개발 문서 (선택적 삭제)
- [ ] `lib/HOW/` 폴더 전체 (11개 파일) - 개발 가이드 문서
- [ ] `_AI_Doc/` 폴더 전체 (18개 파일) - AI 개발 문서
- [ ] `ADMIN_ACCESS.md` - 관리자 접근 URL (보안상 유지 권장)

### 5. 빌드 산출물 (자동 생성)
- [ ] `build/` 폴더 전체 - 빌드 산출물 (자동 생성, .gitignore에 포함)

---

## 🟡 삭제 검토 필요 (사용 여부 확인 필요)

### 1. 테스트 데이터 파일
- [ ] `assets/testcase.json` - 등기부등본 테스트 데이터 (코드에서 사용 중)
- [ ] `assets/testcase.txt` - 등기부등본 테스트 데이터 (코드에서 사용 중)
  - **참고:** `lib/screens/home_page.dart:676`, `lib/api_request/register_service.dart:65`에서 사용
  - **결론:** 개발/테스트용이지만 실제로 사용 중이므로 유지

### 2. 개발 스크립트 (선택적 유지)
- [ ] `copy_apk.ps1` - APK 복사 스크립트 (개발용)
- [ ] `run_app.bat` - 앱 실행 스크립트 (개발용)
- [ ] `deploy_github_pages.bat` - 배포 스크립트 (배포용, 유지 권장)

---

## 🟢 최적화 가능한 부분

### 1. pubspec.yaml 최적화

**주석 처리된 패키지 제거:**
```yaml
# flutter_pdfview: ^1.3.1  # 임시로 주석 처리
```
- [ ] 주석 처리된 패키지 제거 또는 완전 삭제

**미사용 패키지 확인:**
- [ ] `leak_tracker` - 메모리 누수 테스트용 (프로덕션 불필요, 개발용으로 유지 가능)
- [ ] `sqflite`, `sqflite_common_ffi`, `sqflite_common_ffi_web` - SQLite 패키지 (코드에서 사용 안 함 확인 필요)
  - **참고:** `lib/HOW/how_property_management.txt`에서 DatabaseHelper 언급하지만 실제 파일 없음
  - **결론:** 삭제 가능성 높음

**실제 사용 중인 패키지:**
- ✅ `geolocator`, `geocoding` - `lib/screens/propertySale/house_detail_page.dart`에서 사용
- ✅ `file_picker` - `lib/screens/propertySale/electronic_checklist_screen.dart`에서 사용

### 2. 코드 최적화

**사용하지 않는 import 확인:**
- [ ] 전체 파일에서 미사용 import 제거
- [ ] `TODO`, `FIXME` 주석 처리된 코드 확인

**테스트 모드 코드:**
- [ ] `lib/screens/home_page.dart:676` - `useTestcase` 변수 (개발용, 프로덕션에서는 false로 설정)
- [ ] `lib/api_request/register_service.dart:65` - `useTestcase` 변수 (개발용, 프로덕션에서는 false로 설정)

---

## 📊 파일별 상세 분석

### ✅ 실제 사용 중인 파일

1. **테스트 데이터**
   - `assets/testcase.json` - 등기부등본 테스트 데이터 (사용 중)
   - `assets/testcase.txt` - 등기부등본 테스트 데이터 (사용 중)

2. **개발 스크립트**
   - `deploy_github_pages.bat` - 배포 스크립트 (사용 중)

3. **지도 관련**
   - `lib/map/native_naver_map_wrapper.dart` - 네이버 지도 래퍼 (사용 중)
   - `lib/screens/map/naver_map_web_stub.dart` - 웹 스텁 (사용 중)

### ❌ 사용 안 하는 파일

1. **빈 폴더**
   - `lib/database/` - DatabaseHelper 미사용

2. **템플릿 파일**
   - `lib/constants/app_constants_template.dart` - 템플릿 (실제 사용 안 함)

3. **샘플 이미지**
   - `assets/sample_house*.jpg` - 샘플 이미지 (코드에서 참조 안 함)

4. **테스트 파일**
   - `web/test_markers.html` - 네이버 지도 테스트 (개발용)
   - `test/widget_test.dart` - 기본 테스트 (실제 테스트 없음)

5. **Firebase Hosting 기본 파일**
   - `public/index.html` - Firebase Hosting 기본 페이지 (사용 안 함)

---

## 🎯 최적화 우선순위

### 🔴 높은 우선순위 (즉시 삭제 가능)

1. **빈 폴더**
   - `lib/database/` 삭제

2. **미사용 파일**
   - `lib/constants/app_constants_template.dart` 삭제
   - `public/index.html` 삭제
   - `web/test_markers.html` 삭제
   - `test/widget_test.dart` 삭제 (또는 실제 테스트 작성)
   - `assets/sample_house*.jpg` 삭제 (3개)
   - `assets/assets.zip` 삭제

### 🟡 중간 우선순위 (검토 필요)

1. **개발 문서**
   - `lib/HOW/` 폴더 - 개발 가이드 (선택적 삭제)
   - `_AI_Doc/` 폴더 - AI 개발 문서 (선택적 삭제)

2. **개발 스크립트**
   - `copy_apk.ps1` - 개발용 (유지 권장)
   - `run_app.bat` - 개발용 (유지 권장)

3. **pubspec.yaml**
   - 주석 처리된 `flutter_pdfview` 제거
   - `sqflite` 패키지들 삭제 (사용 안 함 확인 후)

### 🟢 낮은 우선순위 (선택적 최적화)

1. **테스트 모드 코드**
   - `useTestcase` 변수를 환경 변수로 변경
   - 프로덕션 빌드 시 자동으로 false로 설정

2. **코드 정리**
   - 미사용 import 제거
   - `TODO`, `FIXME` 주석 처리된 코드 정리

---

## 📝 삭제 체크리스트

### 즉시 삭제 가능
- [ ] `lib/database/` 폴더 삭제
- [ ] `lib/constants/app_constants_template.dart` 삭제
- [ ] `public/index.html` 삭제
- [ ] `web/test_markers.html` 삭제
- [ ] `test/widget_test.dart` 삭제 또는 수정
- [ ] `assets/sample_house.jpg` 삭제
- [ ] `assets/sample_house2.jpg` 삭제
- [ ] `assets/sample_house3.jpg` 삭제
- [ ] `assets/assets.zip` 삭제

### 검토 후 삭제
- [ ] `lib/HOW/` 폴더 (선택적)
- [ ] `_AI_Doc/` 폴더 (선택적)
- [ ] `sqflite` 관련 패키지 (사용 안 함 확인 후)

### 코드 최적화
- [ ] `pubspec.yaml`에서 주석 처리된 패키지 제거
- [ ] 미사용 import 제거
- [ ] 테스트 모드 코드 환경 변수화

---

## 💾 예상 절감 용량

### 즉시 삭제 가능 파일
- 빈 폴더: ~0KB
- 템플릿 파일: ~2KB
- 샘플 이미지 (3개): ~500KB-2MB
- 테스트 파일: ~10KB
- 압축 파일: ~500KB-2MB
- **총 예상:** ~1-4MB

### 선택적 삭제 파일
- 개발 문서: ~500KB-2MB
- AI 문서: ~1-3MB
- **총 예상:** ~1.5-5MB

---

## ✅ 최종 권장사항

### 필수 삭제 (즉시)
1. 빈 폴더 `lib/database/` 삭제
2. 미사용 파일들 삭제 (템플릿, 샘플 이미지, 테스트 파일)
3. `pubspec.yaml`에서 주석 처리된 패키지 제거

### 선택적 삭제 (검토 후)
1. 개발 문서 폴더 (`lib/HOW/`, `_AI_Doc/`) - 필요시 보관
2. 개발 스크립트 (`copy_apk.ps1`, `run_app.bat`) - 개발 편의성 위해 유지 권장

### 유지 권장
1. `deploy_github_pages.bat` - 배포용 스크립트
2. `assets/testcase.json`, `testcase.txt` - 개발/테스트용 (사용 중)
3. `ADMIN_ACCESS.md` - 보안 관련 문서 (유지 권장)

---

## 🎯 요약

**즉시 삭제 가능:** 9개 파일/폴더  
**검토 후 삭제:** 3개 항목  
**코드 최적화:** 3개 항목  

**예상 절감 용량:** ~1-4MB (즉시) + ~1.5-5MB (선택적)

**결론:** 즉시 삭제 가능한 파일들을 먼저 정리하고, 나머지는 검토 후 결정하는 것을 권장합니다.

